Модуль 1. ASGI и Middleware (Глубокое понимание)
✅ Задача 1.1. Написать middleware, который измеряет время ответа 
Что сделать: Создайте middleware, который:

Засекает время до вызова эндпоинта

После вызова вычисляет разницу

Добавляет заголовок X-Response-Time: 123ms

Логирует в консоль: [METHOD] /path - 200 - 123ms

Подвох: Middleware должен работать асинхронно и не блокировать ответ.

Что вы узнаете: Как работает стек middleware, разница между await call_next(request) и обработкой после, как добавлять заголовки в ответ.

✅ Задача 1.2. Написать middleware для обработки исключений
Что сделать: Напишите middleware, который перехватывает любое исключение (включая Exception) и возвращает JSON:

json
{"error": "Внутренняя ошибка сервера", "request_id": "xxx-xxx"}
При этом:

В лог пишется полный стектрейс

Клиенту возвращается только сообщение (без стека)

Статус-код всегда 500 (кроме HTTPException)

Подвох: Если исключение выброшено после того, как начали отправлять ответ — его нельзя перехватить через middleware. Нужно использовать exception_handler.

Что вы узнаете: Разницу между middleware и exception_handler, когда какой инструмент использовать.

✅ Задача 1.3. Написать кастомный роутер с префиксом и тегом
Что сделать: Создайте APIRouter для эндпоинтов /api/v1/users с тегом "Users". Все эндпоинты в этом роутере должны автоматически получать префикс /v1 и тег. Сделайте так, чтобы при добавлении нового эндпоинта тег проставлялся автоматически.

Подвох: APIRouter(prefix="/v1", tags=["Users"]) — но если вы забудете указать тег в эндпоинте, он не появится в документации.

Что вы узнаете: Как работает include_router, наследование тегов и префиксов, как кастомизировать OpenAPI.

Модуль 2. Pydantic v2 (Валидация и сериализация)
✅  Задача 2.1. Валидация на основе других полей (password/confirm)
Что сделать: Создайте модель UserRegister с полями:

password: str

confirm_password: str

email: EmailStr

Валидация:

password минимум 8 символов, хотя бы 1 цифра и 1 заглавная буква (regex)

confirm_password должен совпадать с password

Если не совпадают — ошибка с кодом PASSWORD_MISMATCH

Подвох: Валидатор должен срабатывать после проверки типов, но до сериализации.

Что вы узнаете: Использование @model_validator(mode='after'), кастомные валидаторы с @field_validator, как возвращать структурированные ошибки.

✅   Задача 2.2. Сериализация с вложенными объектами
Что сделать: Есть модели SQLAlchemy:

python
class User(Base):
    id: int
    name: str
    posts: List[Post]

class Post(Base):
    id: int
    title: str
    content: str
    user_id: int
Напишите две Pydantic-схемы:

UserOut — выводит только id и name

UserWithPostsOut — выводит id, name и список постов (только id и title)

Подвох: Схемы должны наследоваться, чтобы не дублировать поля. В эндпоинте вы должны использовать одну схему для списка пользователей, другую — для детального просмотра.

Что вы узнаете: Наследование Pydantic-схем, использование from_attributes, как контролировать глубину сериализации.

✅   Задача 2.3. PATCH с частичным обновлением
Что сделать: Эндпоинт /users/{id} принимает PATCH с телом:

json
{"name": "Новое имя"}  // или {"email": "new@mail.com"}  // или оба
Нужно:

Обновить только переданные поля

Если поле не передано — не трогать его в БД

Если поле передано с null — установить NULL в БД

Вернуть обновленного пользователя

Подвох: Нельзя использовать model_dump() — он вернет все поля, включая дефолтные. Нужен exclude_unset.

Что вы узнаете: Работа с exclude_unset, отличие None от Undefined, как валидировать частичные обновления.

✅   Задача 2.4. Кастомный тип для UUID
Что сделать: Создайте Pydantic-тип UUID7, который:

Принимает строку в формате UUID

Проверяет, что это валидный UUID версии 7

При сериализации возвращает строку без дефисов

Подвох: Стандартный UUID принимает все версии. Нужно написать кастомный валидатор.

Что вы узнаете: Как создавать кастомные типы через Annotated, как использовать __get_pydantic_core_schema__.

Модуль 3. Dependency Injection (Глубоко)
✅   Задача 3.1. Кэширование зависимости внутри запроса
Что сделать: Напишите зависимость get_current_user(token: str = Header(...)), которая:

Декодирует JWT

Достает пользователя из БД

Возвращает объект User

Важно: внутри одного запроса эта зависимость вызывается несколько раз (в разных эндпоинтах и подзависимостях). Нужно сделать так, чтобы БД запрашивалась один раз.

Подвох: Depends(get_current_user) без кэша будет выполнять запрос каждый раз.

Что вы узнаете: Механизм кэширования зависимостей FastAPI, использование @cache или functools.lru_cache для асинхронных функций, работа с request.state.

✅   Задача 3.2. Динамическая зависимость на основе параметра пути
Что сделать: Есть эндпоинт /items/{item_id}. Напишите зависимость get_item(item_id: int, db: Session = Depends(get_db)), которая:

Достает item из БД

Если item не найден — кидает 404

Возвращает объект Item

Эта зависимость должна использоваться в эндпоинте, а также в других зависимостях (например, check_item_owner).

Подвох: Зависимость должна принимать item_id из пути, но не требовать его явной передачи в эндпоинте.

Что вы узнаете: Как параметры пути передаются в зависимости, как работает вложенность Depends.

✅  Задача 3.3. Зависимость с параметрами (фабрика)
Что сделать: Напишите зависимость-фабрику get_paginated_query(model, limit_default=10), которая:

Возвращает функцию, принимающую limit: int = Query(default=...) и offset: int = Query(default=0)

Возвращает объект запроса с примененной пагинацией

Использование:

python
@app.get("/users")
def get_users(pagination: Pagination = Depends(get_paginated_query(User))):
    return pagination.all()
Подвох: Нельзя просто передать параметры в Depends — нужна кастомная фабрика.

Что вы узнаете: Как создавать параметризованные зависимости, как передавать аргументы в Depends, как использовать Callable в зависимостях.

Модуль 4. Базы данных и Транзакции
✅   Задача 4.1. Атомарный перевод денег
Что сделать: Эндпоинт /transfer принимает:

json
{"from_user_id": 1, "to_user_id": 2, "amount": 100}
Нужно:

Проверить, что у отправителя достаточно средств (используя SELECT FOR UPDATE)

Списать с отправителя

Зачислить получателю

Записать в историю транзакций

Всё в одной транзакции. Если ошибка — полный откат.

Подвох: Без FOR UPDATE при конкурентных запросах баланс может стать отрицательным.

Что вы узнаете: Как использовать with_for_update() в SQLAlchemy, управление транзакциями через async with session.begin(), обработка deadlock'ов.

✅  Задача 4.2. Пагинация без OFFSET (курсорная)
Что сделать: Эндпоинт /posts возвращает список постов. Вместо limit/offset используйте курсор:

Запрос: GET /posts?cursor=123&limit=10

Ответ:

json
{
  "items": [...],
  "next_cursor": 456,
  "has_more": true
}
Где cursor — это id последнего полученного поста.

Подвох: Запрос должен быть быстрым на 1 млн записей. OFFSET тормозит.

Что вы узнаете: Как строить запросы WHERE id > cursor ORDER BY id LIMIT n, как работать с составными курсорами (например, по дате + id), индексы.

Задача 4.3. Оптимизация N+1 через selectinload с лимитом
Что сделать: Эндпоинт возвращает список пользователей (50 записей) с их последними 5 постами. Используйте selectinload, но ограничьте количество подгружаемых постов.

Подвох: selectinload по умолчанию подгружает все посты для каждого пользователя. Если у одного пользователя 10 000 постов — ответ будет огромным.

Что вы узнаете: Как использовать selectinload с подзапросом и limit через Query и options, как писать кастомные загрузчики в SQLAlchemy.

Задача 4.4. Soft Delete с фильтрацией
Что сделать: Реализуйте механизм мягкого удаления:

У модели есть поле deleted_at: datetime | None

Все запросы по умолчанию должны исключать удаленные записи (deleted_at IS NULL)

Эндпоинт DELETE устанавливает deleted_at = now()

Эндпоинт должен иметь возможность вернуть удаленные записи через параметр include_deleted=true

Подвох: Нельзя везде вручную добавлять .filter(Model.deleted_at.is_(None)).

Что вы узнаете: Как использовать Query с глобальным фильтром через @app.on_event, написание кастомного Query с методом not_deleted(), использование HybridProperty для фильтрации.

Модуль 5. Фоновые задачи и Асинхронность
Задача 5.1. Фоновая задача с отслеживанием статуса
Что сделать: Эндпоинт /export запускает генерацию CSV (эмуляция: asyncio.sleep(10)) и возвращает task_id. Эндпоинт /status/{task_id} возвращает:

json
{"status": "processing", "progress": 70}
или

json
{"status": "done", "download_url": "/files/export_123.csv"}
Подвох: BackgroundTasks не умеет возвращать статус. Нужно вручную обновлять словарь в app.state.

Что вы узнаете: Как использовать app.state для временного хранилища, как передавать task_id в фоновую функцию, как обновлять прогресс.

Задача 5.2. Отмена фоновой задачи
Что сделать: Добавьте эндпоинт /cancel/{task_id}, который отменяет фоновую задачу. Задача должна корректно обработать отмену (остановиться, не оставив данные в неконсистентном состоянии).

Подвох: Просто task.cancel() не достаточно — нужно, чтобы задача сама проверяла флаг отмены.

Что вы узнаете: Работа с asyncio.CancelledError, использование asyncio.Event для управляемой остановки, graceful cancellation.

Задача 5.3. Ограничение количества одновременных фоновых задач
Что сделать: Ограничьте количество одновременно выполняющихся фоновых задач до 5. Если лимит превышен — возвращайте 429 Too Many Requests.

Подвох: Нужен семафор. Но семафор на уровне приложения должен быть общим для всех запросов.

Что вы узнаете: Использование asyncio.Semaphore, хранение состояния в app.state, как блокировать эндпоинт при достижении лимита.

Модуль 6. Ошибки и Валидация (Продвинутая)
Задача 6.1. Единый формат ошибок с кодом
Что сделать: Все ошибки должны возвращать JSON:

json
{
  "error": {
    "code": "USER_NOT_FOUND",
    "message": "Пользователь с id 123 не найден",
    "details": null
  }
}
Для ошибок валидации Pydantic (422) — details должен содержать список полей с ошибками:

json
{"field": "email", "error": "invalid_email", "message": "Неверный формат email"}
Подвох: Нужно перехватывать ValidationError и парсить его структуру.

Что вы узнаете: Написание кастомного exception_handler, парсинг ValidationError, как добавить кастомные коды ошибок в исключения.

Задача 6.2. Кастомное исключение с автоматическим HTTP-статусом
Что сделать: Создайте класс AppException, который принимает:

status_code — HTTP статус

code — строковый код ошибки

message — сообщение для клиента

details — дополнительные данные

Использование:

python
raise AppException(status_code=409, code="USER_EXISTS", message="Пользователь уже существует")
При этом автоматически должен возвращаться формат из задачи 6.1.

Что вы узнаете: Наследование от HTTPException, кастомные исключения, обработка через exception_handler.

Задача 6.3. Валидация на уровне эндпоинта (не Pydantic)
Что сделать: Есть эндпоинт загрузки файла. Нужно валидировать:

Размер файла ≤ 5 МБ

MIME-тип = image/jpeg или image/png

Имя файла не содержит спецсимволов

Подвох: Это не Pydantic-модель, а UploadFile. Валидация должна быть в эндпоинте или в зависимости.

Что вы узнаете: Работа с UploadFile, как писать зависимости-валидаторы для файлов, как проверять размер без загрузки всего файла в память.

Модуль 7. Streaming и Большие Данные
Задача 7.1. Стриминг большого CSV с сортировкой
Что сделать: Эндпоинт /export/csv отдает 100 000 записей в CSV. Данные должны быть отсортированы по дате создания. Используйте StreamingResponse с генератором, который читает из БД пачками по 1000 записей.

Подвох: Нельзя сделать ORDER BY на всей таблице без индекса — будет тормозить. Нужно использовать курсор (как в задаче 4.2).

Что вы узнаете: Как работает StreamingResponse, использование async generator, как контролировать размер чанка (64 КБ — золотая середина), как комбинировать курсорную пагинацию и стриминг.

Задача 7.2. Стриминг с прогрессом через Server-Sent Events (SSE)
Что сделать: Эндпоинт /stream-progress отправляет клиенту события о прогрессе обработки:

text
data: {"progress": 0, "message": "Начало"}
data: {"progress": 50, "message": "Обработка..."}
data: {"progress": 100, "message": "Готово"}
Используйте EventSourceResponse из sse-starlette.

Подвох: Нужно отправлять события в реальном времени, а не одним куском.

Что вы узнаете: Как работает SSE, отличие от WebSocket, использование yield в генераторе с sleep, как закрыть соединение при завершении.

Задача 7.3. Прокси-запрос с буферизацией
Что сделать: Напишите эндпоинт /proxy, который принимает URL и проксирует запрос к внешнему API. Ответ должен стримиться клиенту без загрузки всего тела в память.

Подвох: Нужно обработать ошибки внешнего API (таймаут, 404) и вернуть корректный статус.

Что вы узнаете: Использование httpx.AsyncClient с stream=True, как пробрасывать статус-код и заголовки, работа с aiter_bytes().

Модуль 8. WebSocket (Опционально, но часто спрашивают)
Задача 8.1. WebSocket-чат с комнатами
Что сделать:

WebSocket эндпоинт /ws/{room_name}

Клиенты подключаются, отправляют сообщения, все в комнате получают

При подключении отправлять последние 50 сообщений из истории (хранить в словаре в памяти)

Подвох: Управление соединениями — словарь room_name: list[WebSocket]. Нужно удалять при разрыве.

Что вы узнаете: Работа с WebSocket, обработка WebSocketDisconnect, использование asyncio.gather для отправки всем, как хранить историю с ограничением размера (deque).

Задача 8.2. WebSocket с аутентификацией
Что сделать: WebSocket принимает токен в query-параметре: ws://localhost/ws?token=xxx. При подключении проверьте токен, и если он невалидный — закройте соединение с кодом 1008.

Подвох: В WebSocket нет заголовков (кроме Sec-WebSocket-Protocol), поэтому токен передают в URL или в протоколе.

Что вы узнаете: Как получить параметры из query в WebSocket, как закрыть соединение с кастомным кодом, отличие от HTTP-аутентификации.

Задача 8.3. Broadcast из HTTP в WebSocket
Что сделать: Есть HTTP-эндпоинт /notify, который принимает сообщение. Все подключенные WebSocket-клиенты должны получить это сообщение (broadcast).

Подвох: Нужен глобальный список всех активных WebSocket-соединений из всех комнат.

Что вы узнаете: Как хранить глобальное состояние (список всех сокетов) в app.state, как отправлять сообщения из HTTP-контекста в WebSocket-контекст (нет проблем — всё в одном цикле событий).

Модуль 9. Безопасность (JWT + Cookies)
Задача 9.1. JWT с access/refresh токенами
Что сделать:

/login — принимает логин/пароль, возвращает access_token (15 мин) и refresh_token (7 дней) в теле ответа

/refresh — принимает refresh_token, возвращает новый access_token

/profile — требует валидный access_token в заголовке Authorization: Bearer <token>

Подвох: Refresh-токен должен быть одноразовым или многоразовым? Нужно решить и объяснить.

Что вы узнаете: Генерация и валидация JWT, алгоритм обновления токенов, как хранить refresh-токены в БД (или нет), как добавить в Depends проверку токена.

Задача 9.2. JWT в HttpOnly Cookie
Что сделать: Переделайте задачу 9.1 так, чтобы:

access_token и refresh_token передавались в HttpOnly Cookie (не в теле)

access_token читался из request.cookies в зависимости get_current_user

Добавлена защита от CSRF через заголовок X-CSRF-Token (сгенерированный на сервере и переданный в отдельной cookie)

Подвох: HttpOnly куки нельзя прочитать из JavaScript, поэтому вы не можете отправить их в заголовке Authorization.

Что вы вы узнаете: Работа с request.cookies, отличие HttpOnly от обычных кук, генерация и валидация CSRF-токена, почему SameSite важен.

Задача 9.3. Rate Limiting на основе пользователя (не IP)
Что сделать: Ограничьте количество запросов для каждого пользователя до 100 в минуту (на основе JWT). Если пользователь не авторизован — лимит по IP (50 в минуту).

Подвох: Хранилище должно быть общим для всех воркеров. Для задачи используйте dict в app.state (в реальности — Redis).

Что вы узнаете: Реализация sliding window, очистка старых записей через asyncio, различие лимитов для авторизованных и неавторизованных.

Модуль 10. Тестирование (pytest)
Задача 10.1. Тестирование эндпоинтов с зависимостями
Что сделать: Напишите тест для эндпоинта /profile, который зависит от get_current_user. В тесте подмените эту зависимость через app.dependency_overrides, чтобы возвращать тестового пользователя, не трогая БД.

Подвох: Зависимость асинхронная, и мок должен быть тоже асинхронным.

Что вы узнаете: Использование dependency_overrides, написание тестов с TestClient (синхронный) и AsyncClient (асинхронный), как подменять зависимости с yield.

Задача 10.2. Тестирование транзакций с откатом
Что сделать: Напишите тест для эндпоинта перевода денег. После каждого теста БД должна возвращаться в исходное состояние (не оставлять изменений). Используйте фикстуру pytest, которая оборачивает тест в транзакцию и откатывает её.

Подвох: SQLAlchemy AsyncSession не умеет откатывать вложенные транзакции так же просто, как синхронная.

Что вы узнаете: Использование savepoint или rollback в тестах, как работает @pytest.fixture с yield, как тестировать конкурентные сценарии.

Задача 10.3. Тестирование фоновых задач
Что сделать: Протестируйте эндпоинт /export, который запускает фоновую задачу. Нужно проверить, что:

Эндпоинт вернул task_id

Через некоторое время задача завершилась и статус изменился на done

Можно скачать файл

Подвох: В тестах нельзя реально ждать 10 секунд. Нужно подменить asyncio.sleep или сделать задачу синхронной в тестах.

Что вы узнаете: Как тестировать асинхронный код с pytest-asyncio, как форсировать выполнение фоновых задач в тестах (передать event_loop и запустить вручную).